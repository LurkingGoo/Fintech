# Cerberus (XRPL Testnet) — Environment Template
# Copy this file to .env.local and fill in real values.
# Never commit .env.local.

# XRPL network config
XRPL_NETWORK=testnet

# XRPL endpoints
# xrpl.js uses WebSocket; keep this public-safe.
XRPL_WS_URL=wss://s.altnet.rippletest.net:51233
# Optional: only needed if you call JSON-RPC directly.
XRPL_RPC_URL=https://s.altnet.rippletest.net:51234

# Admin / issuer secrets (server-side only)
XRPL_ISSUER_SEED=
ADMIN_TOKEN=

# RLUSD (Testnet)
# Published Testnet RLUSD issuer (Ripple docs). Keep as a default; still verify live.
RLUSD_ISSUER=rQhWct2fv4Vc4KRjRgMrxa8xPN9Zx9iLKV
# IMPORTANT: Do not guess the RLUSD currency encoding.
# Discover it live via account_lines and set it here after verifying.
RLUSD_CURRENCY=

# Cerberus gated asset unit token
CERB_ISSUER=
CERB_CURRENCY=CERB

# Optional app config
APP_ENV=local

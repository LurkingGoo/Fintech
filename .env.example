# Cerberus (XRPL Testnet) — Environment Template
# Copy this file to .env.local and fill in real values.
# Never commit .env.local.

# --- Server-side (API routes) ---

# REQUIRED for issuer/admin actions (all /api/admin/* routes except /api/admin/init).
# If you don't have a seed yet, you can generate one via /api/admin/init and then
# paste it here (see docs/SIMULATION.md).
ISSUER_SEED=

# XRPL WebSocket endpoint used by server-side xrpl.js client.
# Optional: defaults to XRPL Testnet.
XRPL_WS_URL=wss://s.altnet.rippletest.net:51233

# --- Client-side (browser) ---

# XRPL WebSocket endpoint used by the browser wallet (public-safe).
# Optional: defaults to XRPL Testnet.
NEXT_PUBLIC_XRPL_TESTNET_ENDPOINT=wss://s.altnet.rippletest.net:51233

# Notes:
# - Do NOT put secrets in NEXT_PUBLIC_* variables.
# - RLUSD currency encoding is discovered live on-ledger; Cerberus avoids hardcoding it.
# - RLUSD Testnet issuer default is built-in: rQhWct2fv4Vc4KRjRgMrxa8xPN9Zx9iLKV
